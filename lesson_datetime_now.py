# This script uses the datetime module to print the current system date and time.
# Demonstrates how to retrieve timestamps using Python's standard library.

from datetime import datetime

current_time = datetime.now()
print(current_time)
